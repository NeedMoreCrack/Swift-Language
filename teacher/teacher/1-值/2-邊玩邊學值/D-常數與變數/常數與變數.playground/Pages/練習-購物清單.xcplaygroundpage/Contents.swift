/*:
## 練習：製作購物清單

 以下常數代表你可能要新增到購物清單中的一些項目：
 */
let eggs = "Eggs"
let milk = "Milk"
let cheese = "Cheese"
let bread = "Bread"
let rice = "Rice"
let newLine = "\n"
//:  - callout(練習：): 使用 `""` 的初始值建立字串變數。將上述每個常數項目新增到清單中，一次新增一個。在項目之間新增 `newLine`。請記得，你可以使用 `+` 運算子結合兩個字串。
var shoppingList = ""
shoppingList += eggs
shoppingList += newLine
shoppingList += milk
shoppingList += newLine
shoppingList += cheese
shoppingList += newLine
shoppingList += bread
shoppingList += newLine
shoppingList += rice
/*:
[上一頁](@previous)  |  第 12 頁，共 13 頁  |  [下一頁：練習：501](@next)
 */