/*:
## 組合字串
程式設計師經常需要將字串組合在一起。

例如，你可能會在社交媒體 app 中看到類似「Chris 按你的貼文讚」的訊息。

在 Swift 中，你可以將字串連在一起來組合字串，也稱為「串連」。
 */
// This might change over time
let username = "Chris"

// This part of the message will get reused
let likesYourPostMessage = "likes your post"

// Concatenate strings by using the plus sign
let finishedMessage = username + " " + likesYourPostMessage
/*:
你可以在結果側邊欄中看到字串是如何組合的。👉

如果你不在兩個字串之間加入空格，會發生什麼事？

 - callout(實驗：): 為你的名和姓宣告 `firstName` 和 `lastName` 的字串常數。\
將它們串連成一個 `fullName` 常數。\
串連你的 `fullName` 和 `likesYourPostMessage`\
在下方的相關註解之下輸入每行程式碼。
 */
// Declare a firstName constant

// Declare a lastName constant

// Combine the strings into a fullName constant

// Combine your full name with likesYourPostMessage

/*:
接著來學習比較複雜的組合字串方式。

[上一頁](@previous)  |  第 6 頁，共 18 頁  |  [下一頁：填空](@next)
 */